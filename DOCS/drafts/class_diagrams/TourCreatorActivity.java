public class TourCreatorActivity extends Activity {
    private Tour tour;
    private LocationClient location;
    private Location currentLocation;

    /*
     * Reads the lat and long from the GPS and saves it in the current
     * location.
     */
    private void recordCoordinates() {
    }

    /*
     * Reads a name from the user and adds it to the tour.
     */
    private void recordName() {
    }

    /*
     * Reads a description from the user and adds it to the tour.
     */
    private void recordDescription() {
    }

    /*
     * Read long description and add to the tour.
     */
    private void recordLongDescription() {
    }

    /*
     * Take image from the camera app and adds it to the current location.
     */
    private void recordImage() {
    }

    /*
     * Reads a description from the user and adds it to the current location.
     */
    private void recordLocationDescription() {
    }

    /*
     * Create the activity.
     */
    protected void onCreate(Bundle savedInstanceState) {
    }
}
